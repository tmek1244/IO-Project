build:
  docker:
    backend: Dockerfile
run:
  backend: python manage.py makemigrations
  backend: python manage.py migrate
  backend: gunicorn IOProject.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: backend
